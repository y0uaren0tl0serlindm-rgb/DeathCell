# DeathCell 设计文档

> 本文件是**策划决策的唯一裁判**。代码实现、美术接入、后续所有取舍都以它为准。
> 每条"已锁定"决策都附理由；要推翻它，先回来改这一条，而不是在别处偷偷绕开。

---

## 1. 一句话定位

类死亡细胞 2D 动作 roguelite：**高速、高惩罚、以构筑差异驱动重玩**。

## 2. 设计支柱

| # | 支柱 | 仲裁问题 |
|---|------|----------|
| 1 | **每局不同** | 它让这一局和上一局有什么不同？ |
| 2 | **读招制胜** | 赢靠读招 + 翻滚，不靠数值；敌人前摇必须高对比（现占位闪黄光，换动画后必须保留提亮/特效） |
| 3 | **快死快回** | 死亡零成本、重开零摩擦、单局紧凑（R 一键重开已是这条的一部分） |
| 4 | **可验证** | 新机制延续团队基因：加了东西就要有对应测试（跳跃模型已有测试是标杆） |

支柱之间冲突时，按编号仲裁：1 > 2 > 3 > 4。

## 3. 宏观流程（v0.1 定稿）

### 3.1 完整循环

```
局外界面（结算 + 解锁 + 选人，一个界面装下）
  ├─ 第 1 局：强制锈剑，无选人界面，直接开局
  └─ 之后：选人 = 选流派，一局到底
        ↓
局内：共 8 层
  ├─ L1~L7：清怪 → 门开 → E 下一层
  └─ L8：终点出口（传送门占位）→ 进门 = 通关
        ↓
通关：额外细胞奖励 → 回局外
        ↓
死亡（任意时刻）：细胞 100% 入账 → 回局外
        ↓
再来一局
```

### 3.2 已锁定决策

| 决策 | 理由 |
|---|---|
| 局内不换人，选人 = 选流派 | 局外选 build、局内调 build（词条），符合 roguelite 惯例 |
| 第 1 局强制锈剑，无选人界面 | 教学局：均衡档教基本功，构筑决策密度为零 |
| 层数 = 8，层深是难度爬坡的锚 | 见 §3.4 |
| 细胞死亡 100% 带出 | 支柱 3"快死快回"：惩罚是"这局没走完"，不在货币上扣；每次死亡都有正向积累 = "再来一把"的发动机 |
| 解锁 = 细胞消费，不设通关条件 | 任何一局的细胞都能攒 |
| 终点先用传送门顶着 | 比"无终点"完整一个量级且零新美术；Boss 以后替换这个位置 |
| 局外单界面：结算 + 解锁 + 选人 | 流程闭环唯一的新 UI，三个功能一个界面装下 |
| 选人界面落地后，局内 K 换武器移除 | 与"局内不换人"一致；现有 K 逻辑正是选人界面的现成功能来源 |

### 3.3 细胞经济（规则已锁，数值待调）

- 获取：局内捡细胞（击杀/精英）+ 通关奖励
- 结算：死亡或通关时 100% 入局外账户，局内只捡不花
- 消费：只在局外界面，解锁角色（双匕 → 重锤）
- **数值（成本、掉落量）等 §3.4 逐层节奏定稿后再调，本轮不碰**

### 3.4 层节奏（原则已定，逐层内容待细化）

- 层深 = 难度爬坡的锚：怪物密度、词条包稀有度权重、精英出现概率全部挂在层数上
- L1 教学层：低密度、无精英
- 怪物引入曲线（§8.4）：L1 只有 Grunt → L2-3 弩手 → L4 跳虫 + Brute 首现 → L5-7 混编 → L8 期末考
- 词条包获取节奏锚在 2 / 4 / 6 层（具体待定，与怪物曲线错开一拍）
- L8 终点层：不再纯堆难度，考验的是"这一局的构筑成没成"

## 4. 角色即流派

**背景**：团队没有角色/武器美术能力，采用网络素材。因此**武器绑定角色**——
不搞"贴图是剑、手感是锤"的假换武器（见 §7 红线）。

**设计定性**：这是架构选择，不是妥协。选人那一刻，节奏、距离、决策空间同时确定
（Hades 式局外选 build）。武器手感与角色身份合一，比死亡细胞的"武器与角色两套系统
互相稀释"更干净。

### 4.1 新角色质量门槛（三条缺一不可）

> 新角色必须同时满足：**节奏不同**（连招时间片有辨识度）、
> **距离不同**（攻击弧线/射程改变站位逻辑）、**决策不同**（战斗时脑子想的事不一样）。
> 只满足两条，不配进阵容。

### 4.2 初始三角（现已作为三把武器存在于代码，手感已调好、有测试锁定）

| 角色 | 节奏 | 距离/决策 | 玩家脑子里想的是 |
|---|---|---|---|
| 锈剑 | 中速两段，节奏均匀 | 中距离，弧线广 | 该打第几段、什么时候收手 |
| 双匕 | 最快取消点，贴身高频 | 最短，贴身 | 什么时候粘、什么时候滚开（缠斗流） |
| 重锤 | 最长前摇/后摇 | 最远最重 | 预判敌人走位，提前挥（走位流） |

差异来自**时间片结构**（前摇/判定/后摇/取消点），不是数值。三套时间片已调好，
角色化时直接沿用，攻击动画必须卡住各段时间片。

> 当前状态：玩家固定锈剑（两段连招），双匕/重锤未启用 —— 这正是 §3.2
> "锈剑独开局"的中间态，选人界面落地时它们就是现成的解锁对象。

### 4.3 角色化后的 meta 线

- meta 解锁对象 = **角色**（不是武器）。
- 现有三把武器代码**不删**：当"流派素材库"保留，新增角色/武器时直接启用。
- K 键退役，空着留给将来的技能槽，不急着填。

## 5. 词条系统设计原则

角色锁死后，"每局不同"的局内部分全压在词条上，品质要求因此高一档：

1. **词条必须改节奏，不只是改数值。**
   - ❌ `+10% 伤害`（不改变任何决策）
   - ✅ `取消点提前 15%`（对双匕是质变、对重锤是量变）
   - ✅ `重锤蓄力段可移动`（直接改写走位逻辑）
2. **同一条词条在不同角色身上价值不同。** 这才叫"这一局和上一局不同"。
3. **掉落物 = 词条包**（白蓝金三档），不是武器掉落。
   拾取视觉用发光球，与细胞同一套，零武器美术需求。
4. 数据结构：`WeaponData` 已是 Resource，加 Affix 数组即可。

## 6. 解锁曲线 = 教学曲线

| 顺序 | 角色 | 教什么 | 解锁成本 |
|---|---|---|---|
| 0 | 锈剑 | 基本功：移动/跳跃/翻滚、连招节奏、读前摇 | 强制开局 |
| 1 | 双匕 | 攻击可以更激进：粘、取消点滚开 | 低（首局死亡即可接近） |
| 2 | 重锤 | 出手要提前量（最难一课） | 高 |

meta 进度同时是教学进度，不额外设计教学系统。

## 7. 设计红线（禁做清单）

1. **禁止"贴图是剑、手感是锤"的假换武器。** 连招时间片是玩家的节奏锚，
   动画对不上时间片，攻击节拍感直接崩（违反支柱 2，包括读自己的招）。
2. **禁止用数值差异冒充流派差异。** 新角色/新词条靠"伤害更高"凑数一律打回（违反支柱 1）。
3. **禁止手推平台高度。** 平台/台阶必须来自 `JumpModel`（issue #7 教训，违反支柱 4）。
4. **禁止破坏前摇可读性。** 任何敌人/玩家动作的前摇必须有高对比视觉信号（支柱 2）。

## 8. 怪物阵容（v0.1）

### 8.1 总纲：一个怪 = 一个决策轴 + 一个教学点

1. **单招原则**：每个怪只有一种攻击。招少而精，玩家才能建立肌肉记忆。
2. **前摇下限 0.4s**：任何攻击前摇 ≥ 0.4s（= 玩家翻滚 0.32s + 反应余量），
   保证"看到信号 → 翻滚"永远来得及。现有 Grunt 0.42 / Brute 0.62 已达标，新怪照此执行。
3. **锁定点在前摇 50%**：朝向/瞄准点在前摇过半时定格，之后玩家可靠移动躲避 ——
   "读招制胜"的兑现点。`enemy.gd` 已有此逻辑，新怪必须沿用。
4. **反制闭环**：每个怪至少两条解法，一条靠"读"（时机）、一条靠"走"（位置），
   且受击可打断攻击（输出有意义）。
5. **错峰出招**：同屏同种怪进入 WINDUP 时，其余同类 0.1~0.25s 内不得进入前摇。
   防"左右同时冲撞"这种无解局面。

### 8.2 读招信号对照表（支柱 2 的落地）

| 怪 | 前摇信号（现占位：颜色脉动） | 前摇 | 攻击 | 后摇 |
|---|---|---|---|---|
| Grunt | 红闪 | 0.42 | 0.14 | 0.50 |
| Brute | 紫蓄力 | 0.62 | 0.18 | 0.70 |
| 弩手 | 弩尖亮星 | 0.50 | 弹丸 | 0.60 装填 |
| 跳虫 | 蹲身压缩 + 亮边 | 0.45 | 抛物线扑击 | 0.40 落地硬直 |

换动画后：四个信号必须形状各异（颜色 / 姿态 / 节奏三选二不同），高对比提亮保留。

### 8.3 单怪规格

**Grunt 冲撞兵（已有，基线怪）**
- 决策轴：距离与时机。教学点：读前摇、翻滚。
- 行为循环：巡逻 → 发现(160px)追击 → 近身(32px)前摇 0.42s（50% 锁朝向）→ 冲撞 170px/s → 0.5s 后摇。
- 数值：40 HP / 12 伤 / 3 细胞。TTK = 一套连招 + 补一刀（12+19.2+12=43.2）。
- 解法：前摇时滚到身后；受击打断攻击。

**Brute 精英（已有，节奏锚点）**
- 决策轴：节奏。教学点：大前摇大惩罚、绕背。
- 数值：110 HP / 22 伤 / 8 细胞。TTK ≈ 4 套连招。
- 0.62s 前摇里玩家能打完一整段连招再滚 —— 它同时是"输出窗口"教学。

**弩手（新增，远程压制者）**
- 决策轴：距离管理。教学点：翻滚穿弹、逼墙。
- 行为循环：保持 ~120px → 瞄准 0.5s（50% 锁瞄准点，弩尖亮星）→ 直线弹丸 260px/s → 装填 0.6s。
  玩家进入 70px → 后退拉开；退到墙边无路可退 = 玩家逼墙成功。
- 数值：30 HP（比 Grunt 软 = 优先目标）/ 10 伤（远程更安全，比 Grunt 轻）/ 4 细胞。
  TTK = 一刀一段、两刀带走（12+19.2=31.2）。
- 解法：翻滚穿弹（无敌帧 0.04~0.24s）/ 跳越弹道 / 逼进死角。
- 弹丸：直线、无追踪、撞地形消失；v0.1 只做水平弹道（可读性优先，不做斜射）。
- 实现：`enemy.gd` 的 WINDUP/ATTACK 语义复用，新增 KEEP_DISTANCE 状态 + 弹丸节点；沿平台巡逻，不主动跨平台。

**跳虫（新增，垂直追击者）**
- 决策轴：垂直。教学点：处理空中目标、惩罚落地。
- 行为循环：同高度巡逻 → 玩家进入 120px 且高度差 ≤48px → 蹲身 0.45s（50% 锁定玩家**当时**位置）
  → 抛物线扑击（落点 = 锁定点）→ 落地硬直 0.4s。
- 数值：45 HP（比 Grunt 略肉，更难被打到）/ 12 伤 / 4 细胞。TTK = 连招 + 两刀。
- 解法：锁定后移动躲开 / 翻滚离落点 / 落地硬直白打。
- **跳跃参数直接复用玩家常数**（JUMP_VELOCITY / GRAVITY）：扑击高度 ≈3.5 格，
  覆盖所有 JumpModel 生成出的平台 —— 根治"敌人不会跳、被地形卡住"。
  这是红线 #3 在敌人侧的延伸。
- 实现：`enemy.gd` 加跳跃物理分支；只在同高度 ±48px 内起跳，杜绝跳崖 / 钻台底。

### 8.4 出现曲线（挂层数，即 §3.4 的怪物部分）

| 层 | 阵容 | 教学点 |
|---|---|---|
| L1 | Grunt | 读前摇 + 翻滚（教学层，低密度无精英） |
| L2-3 | + 弩手 | 切软目标：先杀射手 |
| L4 | + 跳虫，Brute 首现 | 空中目标 / 绕背 |
| L5-6 | 两两混编组合 | 注意力分配 |
| L7 | 高密度混编 | 综合 |
| L8 | 终点层期末考 | 这一局的构筑成没成 |

难度爬坡由 `Game.difficulty_scale()` 承担（血量/伤害随层），密度曲线归房间结构管。

### 8.5 组合设计（1+1>2，同轴不叠加）

- **Grunt + 弩手**（近 + 远）：冲撞逼走位、弹丸罚站桩 → 教"先切软目标"。
- **弩手 + 跳虫**（水平 + 垂直）：弹道封地面路线、扑击赶出掩体 → 教"移动与出手的取舍"。
- **Brute + 任意**：0.62s 前摇是全场节奏锚点，逼注意力分配。
- **禁配**：Grunt + Grunt 同帧冲撞（错峰出招兜底）；双弩手同层必须拉开距离摆放，
  避免弹道完全封死走廊。

### 8.6 数值纪律（改数值前先回来看）

- **TTK 锚定**：小怪 = "一套连招 ± 一刀"。Grunt 43.2 ✓；弩手 31.2 = 两刀 ✓；
  跳虫 43.2+ 略肉 ✓；Brute 例外（精英锚 4 套）。
- **伤害预算**：玩家 100 HP。小怪单次 10~12 ≈ 9 次受击才死（容错高、节奏快）；
  Brute 22 ≈ 5 次（精英压迫感）。
- **前摇下限 0.4s**：= 翻滚 0.32s + 反应余量。
- **细胞梯度**：Grunt 3 / 弩手 4 / 跳虫 4 / Brute 8 —— 越难处理的怪细胞越多。

## 9. 路线图（按依赖顺序）

| 优先级 | 事项 | 理由 |
|---|---|---|
| 1 | **流程闭环**：L8 终点传送门 + 局外界面（结算/解锁/选人）+ 细胞入账 + 移除局内 K | 先让一局有头有尾，meta 线从纸面落到流程上 |
| 2 | **最小构筑系统**：词条包掉落 + 词条作用层 + 角色解锁接存档 | 证明"每局不同"；角色化（美术）可延后，选人界面先用现有三把武器数据顶着 |
| 3 | **远程射手 + 跳虫**（规格见 §8，弩手先行、跳虫次之） | 战斗深度最便宜的一笔；弩手复用基类语义最多，跳虫需要新跳跃物理 |
| 4 | **音效**（命中/翻滚/脚步/死亡 + 敌人前摇提示音） | 放大器，不依赖美术，可并行；前摇提示音属于支柱 2 |
| 5 | **房间结构**：多房间图 | 等前三步有了内容再动，否则只是把一条路画成多格；先定功能类型：战斗/宝箱/精英房 |
| 6 | Boss（替换 L8 传送门）、商店、卷轴 | 给长线玩家的目标，现在做等于给没有留存的游戏做终局 |

## 10. 已知缺口 / 工程事实（与 CLAUDE.md 同步）

- 音效缺失、词条系统待建、每层单房间、`Game.meta_blueprints` 空壳；
  弩手/跳虫已有设计规格（§8）待实现。
- 美术约束：角色/武器素材来自网络，武器必须绑定角色；素材规格见 CLAUDE.md。
